@model Persada.Fr.Model.GEMA_TM_CATEGORY

@{
    ViewBag.Title = "Delete Country";
}

<h2>@ViewBag.Title.</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Category Code
        </dt>

        <dt>
            Category Name
        </dt>

        <dd>
            @Model.CATEGORY_NAME
        </dd>
        <dt>
            Category Description
        </dt>

        <dd>
            @Model.DESCRIPTION
        </dd>
        <dt>
            Url
        </dt>

        <dd>
            @Model.URL
        </dd>

        <dt>
            Class 
        </dt>

        <dd>
            @Model.CLASS
        </dd>
        <dd>
            <img src="@Model.IMAGE_PATH" class="img-medium" />
        </dd>

    </dl>

    @using (Html.BeginForm("ActionDelete", "Category", new { id = Model.ID }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
